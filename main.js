define(["./list"], function(List) {
    
    var Table = {
        
        
    };
    
    /*
        config = {
            
            source: {
                
            },
            bind: [
                {
                    
                }
            ]
        }
    */
    
    return function(config) {
        
        var table = List(config);
        
        return table;
    };
});